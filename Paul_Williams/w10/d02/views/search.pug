<!DOCTYPE html>
html(lang="en")
head
  meta(charset="UTF-8")
  title Movies
body
  h2 search results...
  - for (var i = 0; i < Search.length; i++)
    p
      a(href='movie/' + Search[i]["imdbID"]) #{Search[i]["Title"]}